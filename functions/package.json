{
  "name": "dieselup-functions",
  "private": true,
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": { "build": "tsc", "serve": "npm run build && firebase emulators:start --only functions", "lint": "eslint src --ext .ts", "deploy": "firebase deploy --only functions", "bootstrap-admin": "node scripts/bootstrap-super-admin.mjs" },
  "dependencies": { "firebase-admin": "^13.5.0", "firebase-functions": "^6.4.0", "zod": "^4.1.12" },
  "devDependencies": { "@types/node": "^22.15.0", "typescript": "~6.0.3" }
}
