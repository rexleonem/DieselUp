import { StyleSheet } from 'react-native'; import { router } from 'expo-router'; import { Screen } from '@/components/ui/Screen'; import { Card } from '@/components/ui/Card'; import { Text } from '@/components/ui/Text'; import { Button } from '@/components/ui/Button'; import { spacing } from '@/theme/tokens';
export default function PaymentCallbackScreen() { return <Screen contentStyle={styles.content}><Card style={styles.card}><Text variant="h1">Payment submitted</Text><Text color="#64748B">DieselUp is waiting for Paystack’s signed server confirmation. Your order or wallet will update automatically—this page does not mark payment successful by itself.</Text><Button title="View orders" onPress={() => router.replace('/(app)/(tabs)/orders')} /><Button title="View wallet" variant="outline" onPress={() => router.replace('/wallet')} /></Card></Screen>; }
const styles = StyleSheet.create({ content: { flexGrow: 1, justifyContent: 'center' }, card: { width: '100%', maxWidth: 560, alignSelf: 'center', gap: spacing.lg } });
