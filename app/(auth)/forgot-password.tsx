import { useState } from 'react'; import { StyleSheet } from 'react-native'; import { router } from 'expo-router'; import { Screen } from '@/components/ui/Screen'; import { Card } from '@/components/ui/Card'; import { Text } from '@/components/ui/Text'; import { Input } from '@/components/ui/Input'; import { Button } from '@/components/ui/Button'; import { useAuth } from '@/providers/AuthProvider'; import { colors, spacing } from '@/theme/tokens';
export default function ForgotPasswordScreen() { const { resetPassword } = useAuth(); const [email, setEmail] = useState(''); const [message, setMessage] = useState(''); const [loading, setLoading] = useState(false); const submit = async () => { try { setLoading(true); await resetPassword(email); setMessage('If an account exists, Firebase has sent its password reset email.'); } catch { setMessage('Unable to request a reset. Check the address and try again.'); } finally { setLoading(false); } }; return <Screen contentStyle={styles.content}><Card style={styles.card}><Text variant="h1">Reset password</Text><Text color="#64748B">Enter the email linked to your DieselUp account.</Text><Input label="Email address" keyboardType="email-address" autoCapitalize="none" value={email} onChangeText={setEmail} />{message ? <Text color={message.startsWith('Unable') ? colors.danger : colors.success}>{message}</Text> : null}<Button title="Send reset email" loading={loading} disabled={!email.includes('@')} onPress={submit} /><Button title="Back to sign in" variant="ghost" onPress={() => router.back()} /></Card></Screen>; }
const styles = StyleSheet.create({ content: { flexGrow: 1, justifyContent: 'center' }, card: { width: '100%', maxWidth: 500, alignSelf: 'center', gap: spacing.lg } });
