import { StyleSheet } from 'react-native'; import { Screen } from '@/components/ui/Screen'; import { Card } from '@/components/ui/Card'; import { Text } from '@/components/ui/Text'; import { Button } from '@/components/ui/Button'; import { useAuth } from '@/providers/AuthProvider'; import { spacing } from '@/theme/tokens';
export default function SuspendedScreen() { const { signOut } = useAuth(); return <Screen contentStyle={styles.content}><Card style={styles.card}><Text variant="h1">Account unavailable</Text><Text color="#64748B">This account is suspended. Contact DieselUp support if you believe this is an error.</Text><Button title="Sign out" onPress={signOut} /></Card></Screen>; }
const styles = StyleSheet.create({ content: { flexGrow: 1, justifyContent: 'center' }, card: { width: '100%', maxWidth: 520, alignSelf: 'center', gap: spacing.lg } });
