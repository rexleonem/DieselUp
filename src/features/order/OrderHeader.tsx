import { Pressable, StyleSheet, View } from 'react-native'; import { router } from 'expo-router'; import { Ionicons } from '@expo/vector-icons'; import { Text } from '@/components/ui/Text'; import { useTheme } from '@/hooks/useTheme'; import { colors, spacing } from '@/theme/tokens';
export function OrderHeader({ step, title }: { step: number; title: string }) { const theme = useTheme(); const goBack = () => { if (router.canGoBack()) router.back(); else router.replace('/(app)/(tabs)' as never); }; return <View style={[styles.header, { backgroundColor: theme.colors.background }]}><View style={styles.row}><Pressable accessibilityLabel="Go back" onPress={goBack} style={styles.back}><Ionicons name="arrow-back" size={22} color={theme.colors.text} /></Pressable><View style={styles.copy}><Text variant="caption" color={colors.primary}>STEP {step} OF 5</Text><Text variant="h3">{title}</Text></View><Text variant="caption" color={theme.colors.muted}>{step * 20}%</Text></View><View style={[styles.track, { backgroundColor: theme.colors.border }]}><View style={[styles.progress, { width: `${step * 20}%` }]} /></View></View>; }
const styles = StyleSheet.create({ header: { padding: spacing.lg, gap: spacing.md }, row: { width: '100%', maxWidth: 900, alignSelf: 'center', flexDirection: 'row', alignItems: 'center', gap: spacing.md }, back: { width: 40, height: 40, alignItems: 'center', justifyContent: 'center' }, copy: { flex: 1 }, track: { height: 3, width: '100%', maxWidth: 900, alignSelf: 'center', borderRadius: 2 }, progress: { height: 3, borderRadius: 2, backgroundColor: colors.primary } });
