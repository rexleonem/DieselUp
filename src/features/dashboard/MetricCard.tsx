import { StyleSheet, View } from 'react-native'; import { Ionicons } from '@expo/vector-icons'; import { Card } from '@/components/ui/Card'; import { Text } from '@/components/ui/Text'; import { useTheme } from '@/hooks/useTheme'; import { radii, spacing } from '@/theme/tokens';
export function MetricCard({ label, value, helper, icon, color }: { label: string; value: string; helper?: string; icon: keyof typeof Ionicons.glyphMap; color: string }) { const theme = useTheme(); return <Card style={styles.card}><View style={[styles.icon, { backgroundColor: `${color}18` }]}><Ionicons name={icon} size={20} color={color} /></View><Text variant="caption" color={theme.colors.muted}>{label.toUpperCase()}</Text><Text variant="h2">{value}</Text>{helper && <Text variant="caption" color={theme.colors.muted}>{helper}</Text>}</Card>; }
const styles = StyleSheet.create({ card: { minWidth: 165, flex: 1, gap: spacing.sm }, icon: { width: 38, height: 38, borderRadius: radii.md, alignItems: 'center', justifyContent: 'center' } });
